dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
        maven {
            url 'https://csspeechstorage.blob.core.windows.net/maven/'
        }
    }
}
rootProject.name = "Pack Aloud"
include ':app'
